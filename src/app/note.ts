export class Note {
    public title: string;
    public text: string;
    public description:string;
    constructor(title:string, text:string, description:string) {
        this.title = title;
        this.text = text;
        this.description=description;
    }
  }